import * as React from "react";
import * as ReactDOM from "react-dom";
import FilterableRowTable from './src/List'

ReactDOM.render(
<div>
<FilterableRowTable />
</div>,
  document.getElementById("root")
);